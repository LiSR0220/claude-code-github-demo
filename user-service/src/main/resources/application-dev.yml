# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_db_dev?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root

logging:
  level:
    com.example.userservice: DEBUG